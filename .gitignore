node_modules/
coverage/
.DS_Store

# Local agent tooling and session notes, not part of the published project
.claude/
CLAUDE.md
docs/superpowers/
.playwright-mcp/

# Local arcade artwork and audio. The repository ships original pixel art and a
# synthesised sound bank; a local checkout may drop the cabinet's own sprites
# and samples in here instead (sfx/ for the audio). They are Bandai Namco's and
# must never be committed. See docs/local-art.md and docs/local-audio.md.
assets/local/

# Local reference mirrors of ROM research repos (not redistributed)
reference/
test-results/
